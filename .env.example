# Postgres
POSTGRES_USER=vanja
POSTGRES_PASSWORD=trocar_em_producao
POSTGRES_DB=vanja

# Backend — gere um segredo forte com: openssl rand -hex 32
SECRET_KEY=trocar_em_producao_use_openssl_rand_hex_32
